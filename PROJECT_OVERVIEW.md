# 项目概述 (Project Overview)

## 项目简介

这是 **chenzhigang00** 的个人 GitHub 主页仓库，主要包含个人介绍和算法实现代码。

## 作者信息

- **姓名**: Chen Zhigang (陈志刚)
- **联系方式**: 1312383034@qq.com
- **学校**: 中山大学 (Sun Yat-sen University - SYSU)
- **专业**: 软件工程 (Software Engineering)
- **兴趣方向**: 人工智能 (Artificial Intelligence)
- **当前研究**: 联邦学习 (Federated Learning)

## 项目结构

```
chenzhigang00/
├── README.md              # GitHub 个人主页介绍
├── nyanparrot.gif         # 装饰性动图
└── algorithm/             # 算法实现目录
    └── n-queen/          # N 皇后问题的多种解法
        ├── FIFO_Queue.py                    # FIFO 队列分支限界法
        ├── Priority_Queue.py                # 优先队列分支限界法
        ├── backtrack_PermutationTree.py     # 回溯法-排列树解法
        ├── backtrack_SubsetTree.py          # 回溯法-子集树解法
        └── 绘图1.vsdx                       # 算法图示文档
```

## 核心内容：N 皇后问题 (N-Queens Problem)

### 问题描述

N 皇后问题是一个经典的回溯算法问题：在 N×N 的国际象棋棋盘上放置 N 个皇后，使得它们彼此不能相互攻击。即任意两个皇后都不能处于同一行、同一列或同一斜线上。

### 实现方法

本项目实现了 **4 种不同的解法**，展示了不同算法设计范式：

#### 1. **回溯法 - 子集树** (`backtrack_SubsetTree.py`)
- **算法思想**: 逐行放置皇后，每行尝试 N 个位置
- **搜索空间**: 子集树结构
- **剪枝策略**: 
  - 可行性剪枝：检查列冲突和斜线冲突
  - 对称剪枝：利用棋盘对称性，只搜索一半解空间
- **特点**: 实现简单，易于理解

#### 2. **回溯法 - 排列树** (`backtrack_PermutationTree.py`)
- **算法思想**: 将问题转化为全排列问题
- **搜索空间**: 排列树结构
- **优化**: 
  - 通过交换元素构造全排列
  - 天然满足"每行每列只有一个皇后"的约束
  - 只需检查斜线冲突
  - 使用对称剪枝减少搜索空间
- **特点**: 搜索空间更小，效率更高

#### 3. **分支限界法 - FIFO 队列** (`FIFO_Queue.py`)
- **算法思想**: 使用先进先出队列进行广度优先搜索
- **数据结构**: Python `queue.Queue`
- **特点**: 
  - 按层次遍历搜索树
  - 使用可行性剪枝和对称剪枝
  - 适合找到所有解

#### 4. **分支限界法 - 优先队列** (`Priority_Queue.py`)
- **算法思想**: 使用优先队列（堆）优化搜索顺序
- **数据结构**: Python `heapq` (通过负值实现大顶堆效果)
- **优先级策略**: 深度越深，优先级越高（使用负深度值使小顶堆表现为大顶堆）
- **特点**: 
  - 智能选择扩展节点
  - 可能更快找到解
  - 适用于寻找最优解的问题

### 共同优化技术

所有实现都采用了以下优化：

1. **对称剪枝**: 利用棋盘的对称性，第一行只搜索前半部分位置，然后通过镜像操作得到对称解
2. **可行性剪枝**: 在放置皇后前检查约束条件，避免无效搜索
3. **结果展示**: 以直观的棋盘形式展示所有解（用 "Q" 表示皇后，"." 表示空位）

### 算法比较

| 算法类型 | 时间复杂度 | 空间复杂度 | 适用场景 |
|---------|-----------|-----------|---------|
| 回溯法-子集树 | O(N^N) | O(N) | 小规模问题，易于理解 |
| 回溯法-排列树 | O(N!) | O(N) | 中等规模，效率较高 |
| 分支限界-FIFO | O(N^N) | O(N^N) | 需要所有解 |
| 分支限界-优先队列 | O(N^N) | O(N^N) | 寻找最优解 |

## 使用方法

运行任意一个 Python 脚本：

```bash
cd algorithm/n-queen
python3 FIFO_Queue.py
# 或
python3 Priority_Queue.py
# 或
python3 backtrack_PermutationTree.py
# 或
python3 backtrack_SubsetTree.py
```

然后输入 N 的值（如 4、8 等），程序会输出所有可行解的数量和具体摆放方式。

### 示例

输入 `4` 时，会输出 4 皇后问题的所有解：

```
2
[[2, 4, 1, 3], [3, 1, 4, 2]]

.Q..
...Q
Q...
..Q.

..Q.
Q...
...Q
.Q..
```

## 技术栈

- **编程语言**: Python 3
- **主要库**: 
  - `queue` - FIFO 队列实现
  - `heapq` - 优先队列实现

## 学习价值

这个项目非常适合学习：

1. **算法设计范式**: 回溯法、分支限界法
2. **数据结构应用**: 队列、优先队列、树形结构
3. **优化技巧**: 剪枝策略、对称性利用
4. **Python 编程**: 列表操作、递归、类定义

## 总结

这是一个展示算法思维和编程能力的优秀项目，通过同一问题的多种解法对比，深入理解了不同算法的特点和适用场景。代码清晰，注释详细，是学习经典算法问题的好资源。
